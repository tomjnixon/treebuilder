[
 % useful if the connections are only possible from the device manager to the
 % interface machine (for example if the device manager is behind NAT).
 {pinger, [{ping_hosts, ['treebuilder@tree']}]}
].
